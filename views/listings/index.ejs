<% layout("layouts/boilerplate") %>
<body>
    <h3>All Listings</h3>
    <form method="get" action="/listings/New">
    <button>Create New Listing</a></button>
    </form>
    <ul>
        <% for (let listing of listings) { %>
            <li> <a href="/listings/<%= listing._id %>"><%= listing.title %></a></li>
        <% } %>
    </ul>
</body>

